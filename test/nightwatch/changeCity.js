/* global browser, element, by */

'use strict';

// describe('Given city change from, User', function() {

    // beforeEach(function() {
        // browser.driver.manage().deleteAllCookies();
        // browser.get('/');
    // });

    // var params = browser.params;

    // browser.driver.manage().window().maximize();

    // it('should see new page after reload page', function(done) {
        // var citySelect = element(by.css('.b-main-slogan .chosen-container'));
        // var cityTitle = element(by.css('.b-main-slogan .chosen-container .chosen-single span'));
        // citySelect.click();

        // var cityOption = element.all(by.css('.b-main-slogan .chosen-container .chosen-drop .chosen-results li')).get(1);
        // cityOption.click();

        // expect(cityTitle.getText()).toContain('MÜNCHEN');

        // browser.get('/');

        // expect(cityTitle.getText()).toContain('MÜNCHEN');

        // done();
    // });
// });
